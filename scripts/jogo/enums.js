const tipoInimigo = Object.freeze({
  "slime": 1,
  "wingedSlime": 2,
  "troll": 3
});

const tipoCena = Object.freeze({
  'menu': 0,
  'runner': 1,
  'animacao': 2,
  'gameOver': 999
});

const tipoColisao = Object.freeze({
  "rect": 1,
  "circle": 2
});

const tipoObjeto = Object.freeze({
  "moeda": 1
});

const tipoSentimento = Object.freeze({
  "ideia": 1,
  "amor": 2
});

const tipoProjetil = Object.freeze({
  "magia": 1
});